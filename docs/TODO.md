#TODO

## Slice 1
- [ ] <!--Muz3DilVS-99CqvqdGBmN--> Slice 1 - Workspace Grundgerüst
  - [x] <!--Ia5MfRjquXwT_QctQeE-b--> Projektstruktur für die App initial anlegen
    **Lege eine klare Grundstruktur für das Projekt an, bevor technische Details wachsen. Ziel ist nicht sofort Perfektion, sondern eine belastbare Basis für App, Frontend, Core-Logik und spätere Persistenz. Vermeide zu früh zu viele Unterordner oder Architektur-Spielereien. Wichtig ist vor allem, dass Tauri, React, UI, Core-State und später Dateisystem- oder Terminal-Logik nicht chaotisch vermischt werden.**
  - [x] <!--wb_BowbIDbWSubOaeNnsA--> Tauri-Grundsetup erstellen
    **Erstelle ein lauffähiges Tauri-Basisprojekt, das lokal startbar und entwickelbar ist. Wichtig ist zuerst ein sauberer Startpunkt, nicht sofort vollständige Integration. Prüfe früh, ob Dev-Workflow, Build und lokaler Start stabil funktionieren. Der Fokus liegt hier auf einem funktionierenden Fundament, nicht auf Feature-Tiefe.**
  - [x] <!--CHsHoG9RfzFxgkbFvUbsU--> React-Frontend mit Tauri verbinden
    **Stelle sicher, dass das React-Frontend sauber in die Tauri-App eingebunden ist und der Entwicklungsfluss stabil funktioniert. Ziel ist, dass UI-Entwicklung und Desktop-Hülle nicht getrennte Baustellen bleiben. Achte darauf, dass die Verantwortung klar bleibt: React für Oberfläche, Tauri für Desktop- und Systemnähe. Noch keine unnötigen Bridging-Komplexitäten einbauen.**
  - [ ] <!--sTbPclZ4Hmy7BJ72tDpdw--> grundlegende App-Struktur für das Desktop-Layout definieren
    **Definiere die grobe App-Struktur so, dass Startansicht und Workspace-Ansicht logisch getrennt sind. Es geht hier um die grundlegende Seiten- oder Ansichtslogik, nicht um visuelle Feinarbeit. Wichtig ist, dass das spätere Layout mit File Tree links und Hauptbereich rechts sauber vorbereitet ist. Vermeide zu frühe Detailverliebtheit im Styling.**
  - [ ] <!--MflrD1cmRfA2b9JZL8qxT--> Startansicht für die Projekt- oder Workspace-Liste anlegen
    **Baue eine einfache Startansicht, in der vorhandene Projekte oder Workspaces sichtbar werden. Die Ansicht soll funktional und klar sein, nicht schön um jeden Preis. Wichtig ist, dass Nutzer sofort verstehen, wie sie in einen Workspace einsteigen. Noch keine unnötigen Zusatzinfos, Kartenlogik oder komplexen Filter einbauen.**
  - [ ] <!--T_qBGO_YwJVAbusEnyOlm--> Funktion zum Hinzufügen eines bestehenden lokalen Ordners als Workspace vorbereiten
    **Ermögliche das Hinzufügen eines bereits existierenden lokalen Ordners als Projekt oder Workspace. Das Produkt ist lokal-first, also muss dieser Ablauf früh sauber funktionieren. Achte darauf, dass keine neue Projekt-Erstelllogik eingebaut wird, sondern nur bestehende Ordner eingebunden werden. Fehlerfälle wie ungültige Pfade oder abgebrochene Auswahl sollten sauber behandelt werden.**
  - [ ] <!--dF9SaZg6R40YcyAVzWsgU--> Funktion zum Öffnen eines ausgewählten Projekts vorbereiten
    **Ein ausgewähltes Projekt aus der Liste muss zuverlässig in die Workspace-Ansicht führen. Dieser Übergang ist zentral für den gesamten späteren Arbeitsfluss. Wichtig ist weniger die visuelle Übergangsanimation, sondern dass der richtige Projektkontext sauber gesetzt wird. Halte die Navigation dabei eindeutig und vorhersehbar.**
  - [ ] <!--Rqxz-QJ-mJxoE57TKrDJQ--> Zustand für genau ein aktives Projekt modellieren
    **Modelliere klar, dass im MVP immer nur ein Projekt gleichzeitig aktiv ist. Dieser Zustand sollte zentral und sauber geführt werden, nicht verteilt über UI-Komponenten. Das ist wichtig, damit spätere File-Tree-, Terminal- und Session-Logik auf einem klaren Projektkontext aufbauen. Halte das Modell einfach, aber tragfähig.**
  - [ ] <!--uxCh89J4JzV5m1qbcTPBl--> Grundlayout mit linkem File Tree und rechtem Hauptbereich implementieren
    **Setze das Kernlayout des Produkts um: links Projektstruktur, rechts Hauptarbeitsbereich. Dieses Layout ist ein zentrales Element der Produktidentität und sollte früh testbar sein. Noch keine Perfektion im Resizing oder komplexe Layout-Flexibilität nötig. Wichtig ist vor allem, dass die Fläche logisch, stabil und angenehm nutzbar wirkt.**
  - [ ] <!--4YJeldGFx_HSBATHH_8OP--> initialen Terminal-Tab im Hauptbereich anzeigen
    **Beim Öffnen eines Projekts soll standardmäßig direkt ein Terminal-Tab sichtbar sein. Das unterstützt den terminal-first Charakter des Produkts von Anfang an. Auch wenn das Terminal zu diesem Zeitpunkt noch nicht vollständig ausgebaut ist, sollte die Grundidee bereits sichtbar sein. Dieser Standardzustand ist wichtig für die spätere Nutzererwartung.**
  - [ ] <!--Pmv1BLMK-4OJYxImV09Hc--> File Tree für ein geöffnetes lokales Projekt lesend anbinden
    **Der File Tree soll die Struktur des ausgewählten lokalen Projekts zunächst lesend anzeigen können. Für Slice 1 reicht es, wenn Navigation und Darstellung funktionieren. Noch keine Schreiboperationen oder komplexen Dateiinteraktionen nötig. Wichtig ist vor allem, dass der File Tree korrekt, stabil und projektbezogen arbeitet.**
  - [ ] <!--s4RcRdt_TzZ3lF00yVHYF--> grundlegende Navigation zwischen Startansicht und geöffnetem Workspace umsetzen
    **Die App braucht einen klaren Wechsel zwischen Projektliste und aktivem Workspace. Diese Navigation sollte bewusst simpel gehalten werden, damit der Grundfluss früh verständlich ist. Entscheidend ist, dass Nutzer nicht in undefinierten Zwischenzuständen landen. Der Fokus liegt auf robuster Grundnavigation, nicht auf Komfortfunktionen.**
  - [ ] <!--tTICdUKwxqbbkBuK3MFe_--> prüfen, ob Slice 1 als erster testbarer Stand lokal stabil funktioniert
    **Prüfe am Ende Slice 1 als zusammenhängenden Stand, nicht nur einzelne Teilfunktionen. Ziel ist ein erster belastbarer Meilenstein: Projekt auswählen, öffnen, Grundlayout sehen, File Tree sehen, Terminal-Bereich sehen. Achte darauf, ob sich die App schon jetzt wie ein echter Workspace anfühlt oder noch zu sehr wie ein bloßes Gerüst. Halte auftretende Brüche oder Unklarheiten direkt fest.**
    lice 2
- [ ] <!--sqqfsyqhzvWLg2kp56hHH--> Slice 2 - Terminal First Arbeitskern
  - [ ] <!--tzdJdIDEaGo36a5gcj5YU--> Mehrere individuelle Terminal-Tabs unterstützen
    **Der Nutzer soll mehrere Terminal-Instanzen parallel nutzen können, jede mit eigenem Zustand und Kontext. Das ist wichtig, weil Entwickler oft verschiedene Arbeitsbereiche gleichzeitig offen haben, etwa Build-Prozesse, Dev-Server, Tests oder direkte Befehle. Wichtig ist, dass jeder Tab seinen eigenen Shell-Kontext behält und unabhängig bedienbar ist.**
  - [ ] <!--nZLaAP-PHFNzXKLFAVEO6--> Neuen Terminal-Tab über einen Plus-Button anlegen können
    **Ein klarer Plus-Button oder ein äquivalentes UI-Element soll das Anlegen eines neuen Terminal-Tabs sofort ermöglichen. Das ist das primäre Mittel, um neue Terminal-Sessions zu starten. Es soll einfach, sofort erkennbar und zuverlässig funktionieren. Eine Tastenkombination als Alternative ist später nice-to-have, aber für Slice 2 nicht entscheidend.**
  - [ ] <!--KN6QwfcEI13EKaPkOjYnT--> Terminal-Tabs manuell umbenennbar machen
    **Mehrere offene Terminal-Tabs werden schnell unübersichtlich, wenn sie nur generische Namen tragen. Der Nutzer soll einen Tab gezielt benennen können, um die Orientierung zu behalten. Das ist besonders wichtig, wenn verschiedene Tasks oder Kontexte in separaten Tabs laufen. Die Benennung soll einfach und sofort sichtbar im Tab sein.**
  - [ ] <!--iXC4XqFCWnMwGae2zuHCL--> Tab-Verhalten für mehrere Terminal-Sessions sauber umsetzen
    **Das Schließen, Wechseln und Verhalten von Tabs soll konsistent und vorhersehbar sein. Dazu gehören das Schließen des aktuellen Tabs, das Umschalten zwischen Tabs und das Verhalten beim Letzten Tab. Wichtig ist, dass keine unbeabsichtigten Prozesse oder Zustandsverluste auftreten. Der Nutzer soll sich auf das Tab-Verhalten verlassen können.**
  - [ ] <!--t64ELCYaZFcSLxXQFEB4F--> Sicherstellen, dass das eingebettete Terminal als echter nutzbarer Arbeitskern funktioniert
    **Das Terminal ist der zentrale Arbeitsbereich. Es muss echt, vollständig und zuverlässig funktionieren, nicht nur eine konsolenartige Anzeige sein. Interaktive CLI-Tools, shellspezifische Eingaben, langlaufende Prozesse und normale Befehle müssen ohne Einschränkungen funktionieren. Das ist die nicht verhandelbare Kernanforderung für diesen Slice.**
  - [ ] <!--iyeoAw-cJ_DTrV0DOO8Jo--> Prüfen, ob Slice 2 als terminal-first Arbeitsumgebung glaubwürdig und stabil nutzbar ist
    **Am Ende von Slice 2 soll das Terminal als ernsthafter Arbeitsbereich getestet werden. Prüfe, ob sich das Verhalten wie ein echtes Terminal anfühlt, nicht wie eine Notlösung. Achte besonders auf Reaktionsfähigkeit, Interaktivität, Shell-Kompatibilität und ob das Öffnen, Schließen und Wechseln von Tabs zuverlässig funktioniert. Halte fest, was überzeugt und wo noch Reibungspunkte liegen.**
    lice 3
- [x] <!--RVNydLfuDTslrOno-vTIi--> Slice 3 - Dateien direkt im selben Raum
  - [x] <!--Aq2E5cdUnQs2ZezWZhz3w--> Dateien aus dem File Tree im Hauptbereich öffnen können
    **Klicken auf eine Datei im File Tree soll diese im Hauptbereich als Tab öffnen. Das ist der zentrale Mechanismus, um vom Projektkontext zur Datei zu kommen, ohne die App oder den Workflow zu wechseln. Wichtig ist, dass das Öffnen zuverlässig und ohne spürbare Verzögerung funktioniert. Für Slice 3 reicht das Öffnen und Darstellen, noch keine Bearbeitung.**
  - [x] <!--TBlfNyzIHgPHPpnOqny9D--> Mehrere Datei-Tabs unterstützen
    **Mehrere Dateien sollen gleichzeitig als Tabs offen sein können. Der Nutzer soll zwischen ihnen wechseln können, ohne die jeweils andere zu verlieren. Das ist wichtig für den Arbeitsfluss, wenn mehrere Dateien gleichzeitig relevant sind. Jeder Tab soll seinen eigenen Dateikontext und Zustand behalten.**
  - [x] <!--qmBgVJGtzhNl3db37g5Wh--> Textbearbeitung für normale Alltagsfälle umsetzen
    **In den geöffneten Dateien soll Textbearbeitung möglich sein. Für Slice 3 reicht normale Alltagsbearbeitung: Tippen, Löschen, Markieren, Ersetzen im Rahmen üblicher Editor-Nutzung. Kein vollständiger IDE-Editor, aber ausreichend für die meisten direkten Arbeiten an Code oder Textdateien.**
  - [x] <!--S0RURDIIVsgSC5UJILIBI--> Manuelles Speichern als Standard implementieren
    **Änderungen sollen manuell gespeichert werden, nicht automatisch. Das gibt dem Nutzer Kontrolle und passt zum klaren Produktversprechen. Der Standardfall ist: Nutzer arbeitet, entscheidet selbst über Speichern. Das ist gleichzeitig einfacher umzusetzen und für erfahrene Entwickler vertraut.**
  - [x] <!--GfR2jOPgZAPSEryYot3t2--> Undo und Redo unterstützen
    **Undo und Redo für Bearbeitungshandlungen müssen zuverlässig funktionieren. Das ist eine Grundanforderung an jeden Texteditor. Wichtig ist, dass der Undo-Verlauf bei Dateiwechsel oder Tab-Wechsel erhalten bleibt, damit der Nutzer nicht ungewollt Arbeitsschritte verliert.**
  - [x] <!--8HNukxFuwlhzVHEYachFY--> Syntax Highlighting je nach Sprache integrieren
    **Der Editor soll Code für die erkannte Sprache farblich hervorheben. Das ist für die Lesbarkeit und das Arbeiten mit Code zentral. Eine zu diesem Zeitpunkt ausreichende Breite an unterstützten Sprachen ist wichtig, aber keine vollständige Abdeckung aller existierenden Sprachen. Der Fokus liegt auf den gängigsten: JavaScript, TypeScript, Python, HTML, CSS, JSON, Markdown und ähnliche.**
  - [x] <!--WUZ-pCXkYggiV8TEbBObU--> Zeilennummern im Editor anzeigen
    **Zeilennummern sollen im Editor sichtbar sein. Das ist für Entwicklerarbeit eine grundlegende Anforderung, besonders bei Fehlermeldungen, bei der Navigation in größeren Dateien und beim Arbeiten mit Logs oder Tracebacks. Das Feature ist technisch trivial umzusetzen und für die Nutzererwartung wichtig.**
  - [x] <!--HeHwwl4icYbS463-_cD8Q--> Suche innerhalb der geöffneten Datei implementieren
    **Innerhalb einer geöffneten Datei soll der Nutzer nach Text suchen können, typischerweise über Strg oder Cmd+F. Das ist eine der am häufigsten genutzten Editor-Funktionen. Wichtig ist, dass die Suche schnell und zuverlässig funktioniert und die Treffer übersichtlich dargestellt werden.**
  - [ ] <!--yXtcio-kjXWO4oXj5MAob--> Prüfen, ob Terminal und Datei-Arbeit ohne Toolbruch im selben Arbeitsraum überzeugend funktioniert
    **Am Ende von Slice 3 soll der zusammenhängende Arbeitsfluss aus Terminal und Datei-Arbeit getestet werden. Zentrale Frage: Fühlt sich das Arbeiten mit beiden Bereichen im selben Workspace wirklich besser an als ständiges Wechseln zwischen Apps? Achte besonders darauf, ob der Übergang zwischen Terminal und Datei-Tab reibungslos ist und ob die Arbeit in beiden Bereichen gleichermaßen flüssig funktioniert.**
    lice 4
- [ ] <!--LS2IQg10dcgF_0VjyHBHD--> Slice 4 - File Tree als Arbeitswerkzeug
  - [ ] <!--NvWn8wlo-3OZHW8RhmeMM--> Erstellen neuer Dateien aus dem File Tree unterstützen
    **Der Nutzer soll direkt aus dem File Tree heraus neue Dateien anlegen können, ohne den Umweg über das Terminal oder eine externe Anwendung. Das ist ein zentraler Schritt, um den File Tree vom reinen Navigator zum echten Arbeitswerkzeug zu machen. Wichtig ist, dass der neue Dateiname sofort vergebbar und die Datei direkt im Anschluss editierbar ist.**
  - [ ] <!--z_8xVYd4LAlflcxWu4Sl7--> Erstellen neuer Ordner aus dem File Tree unterstützen
    **Analog zum Anlegen neuer Dateien soll der File Tree auch das Erstellen neuer Ordner ermöglichen. Das unterstützt die vollständige Strukturarbeit innerhalb der App. Auch hier ist wichtig, dass der neue Ordner sofort verfügbar und nutzbar ist, ohne die App oder den Workflow wechseln zu müssen.**
  - [ ] <!--fcSY2gX14KH5oKfzcf7h0--> Umbenennen von Dateien und Ordnern ermöglichen
    **Der Nutzer soll direkt im File Tree Dateien und Ordner umbenennen können. Das ist eine grundlegende Strukturoperation, die ohne externen Dateimanager funktionieren muss. Wichtig ist, dass die Umbenennung zuverlässig ist, keine unbeabsichtigten Nebeneffekte hat und der Kontext, etwa geöffnete Tabs, erhalten bleibt.**
  - [ ] <!--ll4wBjGK93FSBpu0z-sz9--> Löschen von Dateien und Ordnern mit Bestätigung umsetzen
    **Löschen soll mit einer Bestätigungsabfrage geschehen, bevor die Operation ausgeführt wird. Das ist wichtig, weil versehentliches Löschen im Projektkontext deutlich schmerzhafter ist als im normalen Dateisystem. Die Bestätigung soll klar kommunizieren, was gelöscht wird, und einen Abbruch leicht möglich machen.**
  - [ ] <!--6W2GNXsPNjD2a8Lr_F2oq--> Drag and Drop zum Verschieben im File Tree unterstützen
    **Der Nutzer soll Dateien und Ordner per Drag and Drop im File Tree verschieben können. Das ist für die Strukturarbeit ein deutlicher Komfortgewinn und entspricht dem natürlichen Workflow in Dateimanagern. Wichtig ist, dass die Zielposition eindeutig erkennbar ist und die Operation zuverlässig ausgeführt wird.**
  - [ ] <!--65xINYWsi5xuPbA6UbhUw--> Fehlerfälle bei Dateioperationen sauber behandeln
    **Alle Dateioperationen sollen auch bei Fehlern sauber abgefangen werden: ungültige Namen, fehlende Schreibrechte, nicht gefundene Pfade, gleichzeitige Zugriffe oder abgebrochene Operationen. Der Nutzer soll in jedem Fall eine klare, verständliche Rückmeldung bekommen, was passiert ist und wie er weiterkommt.**
  - [ ] <!--5YxamI4YKx5UZIkOWEbWM--> Prüfen, ob Strukturarbeit ohne externen Dateimanager zuverlässig funktioniert
    **Am Ende von Slice 4 soll der File Tree als vollständiges Strukturierungswerkzeug getestet werden. Zentrale Frage: Kann der Nutzer alle wesentlichen Datei- und Ordneroperationen durchführen, ohne die App zu verlassen oder einen externen Dateimanager zu benutzen? Achte besonders darauf, ob die Operationen zuverlässig, die Fehlerbehandlung verständlich und die Übergänge zwischen Aktionen reibungslos sind.**
    lice 5
- [ ] <!--cgPCniT-3ToDWSPfK3q51--> Slice 5 - Session Änderungskern
  - [ ] <!--45kttUJwvPZjCqZp2Rv8K--> Beim Öffnen eines Projekts automatisch eine neue Session anlegen
    **Sobald der Nutzer ein Projekt öffnet, soll automatisch eine neue Session erzeugt werden — ohne manuellen Auslöser. Das ist das Grundprinzip der Session-Logik: Jedes Projekt öffnen erzeugt einen neuen Eintrag in der Session-Historie. Wichtig ist, dass dieser Startpunkt zuverlässig und ohne spürbare Verzögerung passiert, damit der Nutzer direkt in einem sauberen Session-Kontext arbeitet.**
  - [ ] <!--c9DAM9QSlWPKD2dTQqrmg--> Aktive Session sauber im zentralen State führen
    **Die aktive Session muss an einer zentralen, klar definierten Stelle im State verwaltet werden — nicht verteilt über mehrere Komponenten. Alle anderen Funktionen (Änderungserfassung, Diff, Markierung im File Tree) greifen auf diesen einen State zu. Das ist die Grundlage für alles Weitere und muss daher sauber und verlässlich funktionieren.**
  - [ ] <!--Z2em9P-QgsQktag2zn5gP--> Änderungen der aktuellen Session erfassen
    **Das System soll alle Änderungen an Dateien während einer aktiven Session mitverfolgen: neu erstellte, bearbeitete und gelöschte Dateien. Die Erfassung soll im Hintergrund passieren, ohne den Nutzer zu unterbrechen. Wichtig ist, dass die Zuordnung zuverlässig ist — also keine Änderungen verloren gehen oder falsch zugeordnet werden, auch wenn der Nutzer mehrere Dateien parallel bearbeitet.**
  - [ ] <!--CPZSGka1InQU2n7xl2M2e--> Geänderte Dateien der aktuellen Session im File Tree markieren
    **Dateien, die während der aktuellen Session geändert wurden, sollen im File Tree visuell erkennbar sein — dezent, aber eindeutig. Das gibt dem Nutzer sofort eine Orientierung darüber, wo in seinem Projekt er gerade gearbeitet hat. Die Markierung soll nach dem Speichern einer Datei sofort sichtbar werden und beim Schließen des Projekts zurückgesetzt werden.**
  - [ ] <!--oiMfJof7YwdxweU7pe5fq--> Einen eigenen Diff- oder Änderungstab für die aktuelle Session anlegen
    **Im Hauptbereich soll ein eigener Tab existieren, der alle Änderungen der aktuellen Session zusammengefasst darstellt. Dieser Tab ist vom Datei-Tab und Terminal-Tab getrennt und bietet eine eigene Ansicht. Wichtig ist, dass der Tab früh angelegt wird und einfach zugänglich ist — der Nutzer soll nicht lange suchen müssen, um seine Änderungen einzusehen.**
  - [ ] <!--Rnt_aA8NKLoxcoz-SWi8H--> Die Änderungsansicht als read-only umsetzen
    **Die Diff-Ansicht zeigt Änderungen, soll aber nicht editierbar sein. Der Nutzer sieht seine Änderungen, bearbeitet sie aber nicht in diesem Tab. Das reduziert die Komplexität und entspricht dem Produktversprechen: die Änderungsansicht ist ein Überblick, kein Editor. Editieren geschieht in den Datei-Tabs.**
  - [ ] <!--yrbD56Mb8Hp8pfGXESIMp--> Alt- und Neu-Vergleich für geänderte Inhalte darstellen
    **In der Diff-Ansicht soll der Nutzer sowohl den ursprünglichen Zustand einer Datei als auch den aktuellen Zustand sehen können — idealerweise als Alt-/Neu-Vergleich oder als unified Diff. Das ist das zentrale Feature der Änderungsansicht und muss für jede geänderte Datei funktionieren, die während der Session bearbeitet wurde.**
  - [ ] <!--ZmL9RVYEvkRoIqabzGPl8--> Sicherstellen, dass die Session-Änderungslogik im Alltag verständlich und verlässlich funktioniert
    **Am Ende von Slice 5 soll die gesamte Änderungserfassung und -ansicht im realen Arbeitsfluss getestet werden. Zentrale Fragen: Werden Änderungen korrekt und vollständig erfasst? Ist die Diff-Ansicht schnell und übersichtlich? Versteht der Nutzer intuitiv, was die Session-Änderungslogik für seinen Arbeitsfluss bedeutet? Halte fest, wo die Logik überzeugt und wo es noch Reibung gibt.**
    lice 6
- [ ] <!--4uyp632XVcakJUYtrt_JW--> Slice 6 - Session Historie
  - [ ] <!--gQwjkvZtCQQFcH3fOBQeC--> Liste alter Sessions anzeigen
    **Alle vergangenen Sessions sollen in einer Liste sichtbar sein, aufrufbar und von der aktuellen Session unterscheidbar. Die Liste soll chronologisch sortiert sein, die aktuellste oben. Wichtig ist, dass die Liste auch nach längerer Zeit noch verständlich bleibt — der Nutzer soll schnell die gesuchte Session wiederfinden können, auch wenn viele vergangene Sessions existieren.**
  - [ ] <!--fd6UkTDWfL_tMZeTaAXfA--> Pro Session Name und Zeitstempel anzeigen
    **Jede Session soll mit ihrem Namen und einem klaren Zeitstempel angezeigt werden. Der Zeitstempel gibt die Orientierung, der Name die inhaltliche Einordnung. Zusammen sollen diese beiden Informationen ausreichen, um eine Session aus der Liste zu identifizieren, ohne sie öffnen zu müssen. Das ist das zentrale Informationsminimum für die Session-Liste.**
  - [ ] <!--XqHPQI4CemBEBZpwPE-oY--> Session-Namen automatisch erzeugen
    **Beim Anlegen einer neuen Session soll automatisch ein Name generiert werden, der die Session schnell identifizierbar macht. Idealerweise basiert der Name auf einem erkennbaren Merkmal — etwa dem geöffneten Projektordner, einem Git-Branch oder einem Zeitstempel. Wichtig ist, dass der automatische Name bereits beim Anlegen sichtbar ist und sofort eine grobe Orientierung gibt.**
  - [ ] <!--d1xP8ye5VsiRnQk6BpJjG--> Session-Namen manuell änderbar machen
    **Der Nutzer soll den automatisch generierten Session-Namen jederzeit manuell überschreiben können. Das ist wichtig, weil automatische Namen nur eine grobe Orientierung bieten. Ein eigener Name macht eine Session deutlich schneller wiederauffindbar — besonders nach längerer Zeit oder bei mehreren Sessions pro Projekt. Die Änderung soll einfach und sofort sichtbar sein.**
  - [ ] <!--lj7urmYQw-kj-Ext5NwQc--> Fallback für Session-Namen mit Git-Branch und Zeitstempel berücksichtigen
    **Wenn keine automatische Namenslogik sinnvoll möglich ist — etwa weil das Projekt kein Git-Repository ist — soll ein Fallback greifen: Git-Branch-Name plus Zeitstempel. Das stellt sicher, dass jede Session einen brauchbaren Standard-Namen hat, auch ohne zusätzliche Konfiguration. Der Nutzer kann dann immer noch manuell anpassen.**
  - [ ] <!--oXQYt1RBBrG1sorAls1jS--> Alte Sessions wieder öffnbar machen
    **Der Nutzer soll eine alte Session aus der Liste auswählen und wieder öffnen können. Das Öffnen einer alten Session stellt den Projektkontext so weit wie möglich wieder her — also welche Dateien damals geöffnet waren und welche Änderungen gemacht wurden. Wichtig ist, dass das Öffnen alter Sessions keine bestehende aktuelle Session überschreibt, sondern eine eigene Ansicht oder einen Wechsel ermöglicht.**
  - [ ] <!--avd9sKGrCdAdDVbxKIRZu--> Beim Öffnen alter Sessions den Fokus auf Änderungen und Dateien legen
    **Wenn eine alte Session geöffnet wird, soll der Blick direkt auf die damals vorhandenen Änderungen und Dateien gerichtet sein — nicht auf eine leere Oberfläche. Der Nutzer sucht in alten Sessions vor allem eines: Was habe ich damals gemacht und wo bin ich geblieben? Die Diff-Ansicht und die Liste der bearbeiteten Dateien sind daher die zentralen Anlaufstellen beim Öffnen einer alten Session.**
  - [ ] <!--kiIIp4EeiOfV0p8Di-T7Q--> Sicherstellen, dass frühere Arbeitsstände nachvollziehbar wiedergefunden werden können
    **Am Ende von Slice 6 soll die Session-Historie als Ganzes getestet werden. Zentrale Frage: Kann der Nutzer eine Session, die er vor Tagen oder Wochen hatte, schnell und zuverlässig wiederfinden und verstehen, was er damals gemacht hat? Achte besonders darauf, ob die Kombination aus Namen, Zeitstempel und Diff-Ansicht zusammen ein ausreichend vollständiges Bild der vergangenen Arbeit erzeugen. Halte fest, wo noch Lücken oder Unklarheiten bestehen.**